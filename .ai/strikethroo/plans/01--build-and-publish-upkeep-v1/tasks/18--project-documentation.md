---
id: 18
group: "release"
dependencies: [6, 13, 14, 15, 16]
status: "completed"
created: 2026-07-29
skills:
  - technical-writing
complexity_score: 3
---
# Write the documentation for both repositories

## Objective
Produce the plan's documentation set: the `owenbush/upkeep` README (install, cockpit setup, registry config, PAT setup, full command reference, DA-policy stance, degraded-path behavior), the `owenbush/ddev-upkeep` README per the add-on template's conventions (install, commands, fixture model, sanitization guidance), the standalone `tests/fixtures/` convention section, an AI-facing agent-instructions file in each repo, and design-doc updates for any section-13 answers not yet folded in.

## Skills Required
`technical-writing` — all content documents already-built behavior; no code changes beyond doc files.

## Acceptance Criteria
- [ ] `README.md` in this repo covers: Composer global install, `upkeep init` cockpit setup with a complete registry example, PAT acquisition (Drupal.org → Git access) and configuration, every shipped command with its flags (cross-checked against `bin/upkeep list` output — no missing or phantom commands), the human-triggered-merge policy stance, and what happens when API endpoints are closed.
- [ ] `README.md` in the add-on repo follows the template's README structure and covers all shipped commands (cross-checked against the add-on's command files), the dump-vs-snapshot fixture model, and sanitization/size guidance for committed fixtures.
- [ ] The `tests/fixtures/` convention is a self-contained section (in the add-on README) usable by a module maintainer who doesn't run Upkeep: path convention, naming, sanitization mandate, leanness guidance.
- [ ] Each repo has a `CLAUDE.md` (or `AGENTS.md`) stating: layout, how to run tests, and — for this repo — the adapter boundary rule (engine specifics live only in the adapter namespace).
- [ ] `docs/contrib-maintainer-design.md` section 13 has no remaining unresolved bullets (every one carries its verified answer from tasks 1–3 or a pointer to where it was resolved).
- [ ] Every command invocation shown in either README executes successfully as written (run each; a doc that lies fails this task).

Use your internal Todo tool to track these and keep on track.

## Technical Requirements
- Markdown only; match the add-on template's existing README skeleton rather than replacing it.
- Command reference generated by walking the real command list, not from memory.

## Input Dependencies
- All user-facing behavior finalized: tasks 5–6 (add-on), 13–16 (orchestrator commands). Verification answers from tasks 1–3 for the design-doc closeout.

## Output Artifacts
- READMEs, agent-instruction files, the fixtures convention, and the closed-out design doc — the documentation surface task 19 publishes.

## Implementation Notes
Documentation is explicitly a plan deliverable (and the fixtures convention is framed as a community contribution in its own right) — treat the "every shown command runs" criterion as hard: execute each snippet before committing.

<details>
<summary>Detailed steps</summary>

1. Generate the orchestrator command inventory from `bin/upkeep list --format=...`/`--help` output per command; write the reference from it.
2. Write this repo's README in the order a new user needs: install → PAT → `upkeep init` → register modules → dashboard → check/review → merge --fast-lane → notes → status/prune. Include the policy paragraph (why merges are one-at-a-time; unattended mode requires DA approval and is not in v1).
3. Update the add-on README in place; add the fixtures-convention section with its own heading so it is linkable.
4. Write both agent files (short: layout, test command, boundary rules).
5. Sweep `docs/contrib-maintainer-design.md` section 13 for unresolved bullets; fold in remaining answers.
6. Execute every documented command snippet once; fix docs (or surface bugs) on any mismatch.
</details>
